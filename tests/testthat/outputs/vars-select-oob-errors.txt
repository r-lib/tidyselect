> # Bare names
> vars_select(letters, foo)
Error: Must subset existing columns.
x Can't subset column with unknown name `foo`.

> vars_select(letters, -foo)
Error: Must subset existing columns.
x Can't subset column with unknown name `foo`.

> # Names
> vars_select(letters, "foo")
Error: Must subset existing columns.
x Can't subset column with unknown name `foo`.

> vars_select(letters, a:"foo")
Error: Must subset existing columns.
x Can't subset column with unknown name `foo`.

> # Locations
> vars_select(letters, 30, 50, 100)
Error: Must subset existing columns.
x Can't subset locations 30, 50, and 100.
i There are only 26 columns.

> vars_select(letters, -100)
Error: Must subset existing columns.
x Can't subset location 100.
i There are only 26 columns.

> vars_select(letters, !100)
Error: Must subset existing columns.
x Can't subset location 100.
i There are only 26 columns.

